{
  "$schema": "https://opencode.ai/config.json",

  // Container-global runtime config for the shared OpenCode service.
  // Keep shared plugins here so generated agent workspaces stay minimal.
  "plugin": [
    "@aeondave/opencode-dotenv@latest",
    "opencode-database-plugin@1.0.12"
  ],
  "lsp": {
    "pyright": {
      "command": [
        "/opt/vibetrading/analysis/.venv/bin/pyright-langserver",
        "--stdio"
      ],
      "extensions": [".py", ".pyi"],
      "env": {
        "VIRTUAL_ENV": "/opt/vibetrading/analysis/.venv"
      }
    }
  }
}
