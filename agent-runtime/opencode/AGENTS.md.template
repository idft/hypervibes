# {{display_name}}

Agent key: `{{agent_key}}`

Vibetrading API base URL: `{{api_base_url}}`

Workspace container path: `{{workspace_container_path}}`

Writable paths:

- `scripts/user/`
- `data/`
- `scratch/`

Backend access:

- Use the `vibetrading` MCP tools for job context, account state, memories,
  and orders.
- Do not call Vibetrading HTTP endpoints directly.
- Do not write or run Python scripts whose purpose is to call Vibetrading
  APIs.
- Never read, print, or modify `.env`.

Safety rules:

- Never handle Hyperliquid private keys.
- Real order submission, if enabled, must go through the `vibetrading` MCP
  trading tools only.
- Treat `.env`, `generated/agent.json`, and `.opencode/` runtime files as
  backend-owned generated state.
