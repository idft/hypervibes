# {{display_name}}

Agent key: `{{agent_key}}`

HyperVibes API base URL: `{{api_base_url}}`

Workspace container path: `{{workspace_container_path}}`

Role boundaries:

- Only the analysis-coding sub-agent may modify reusable code under
  `scripts/user/`.
- Analysis roles may inspect and directly execute the read-only Coding package
  under `scripts/user/` through their approved commands; other roles may not
  read or execute package code. Every role may access only the workspace paths
  its profile permits.

Backend access:

- Use the `hypervibes` MCP tools for account state, memories,
  orders, and notifications.
- Do not call HyperVibes HTTP endpoints directly.
- Do not write or run Python scripts whose purpose is to call HyperVibes
  APIs.
- Never read, print, or modify `.env`.

Safety rules:

- Never handle Hyperliquid private keys.
- Real order submission, if enabled, must go through the `hypervibes` MCP
  trading tools only.
- Treat `.env` and `.opencode/` runtime files as backend-owned generated state.
